// Copy to sync-config.js (gitignored) and fill in. Baked into the APK as the default sheet connection.
window.GL_SYNC = {
  script: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',
  secret: 'same-as-SECRET-in-Code.gs'
};
